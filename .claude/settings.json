{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "file=$(jq -r '.tool_input.file_path // empty' 2>/dev/null); if [[ \"$file\" =~ \\.(ts|tsx)$ ]]; then cd /Users/kwoncheuljin/personal/pdf-studio-desktop && pnpm typecheck 2>&1 | head -20; fi"
          }
        ]
      }
    ]
  }
}
